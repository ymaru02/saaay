echo "---build start---"

stage('Checkout Stage') {
    echo "---Checkout---"
        npm install
}

stage('Build Stage') {
    echo "---Build Stage---"
        npm run build
}

stage('Serve Stage') {
    echo "---Push Stage---"
}